# farm-shared

Cross-package contracts for FARM: capability card and run record schemas, the error catalog, and wire protocol versions. Both the Edge Agent and the Cloud worker import from here so the two sides cannot drift.
