# farm-cloud

The hosted half of FARM. The `worker/` subdirectory holds the Cloudflare Worker that routes planning calls and brokers WebSocket dispatch; `ui/` holds the live-dashboard Pages app. Both are placeholders today and get filled in by later tasks (006 for the worker, 007 for the UI).
