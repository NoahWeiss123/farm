# Worktrees and runtime logs
worktrees/
logs/

# Local-only orchestration layer (does not ship)
AGENTS.md
LAUNCH.md
bin/
tasks/
.githooks/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Node / Bun
node_modules/
.next/
out/
.bun/
*.tsbuildinfo

# Cloudflare
.wrangler/
.dev.vars

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Local state and secrets
.env
.env.local
.farm/
*.pem
*.key

# Demo data and artifacts (committed via release tags, not main)
runs/
checkpoints/
*.mp4

# Teleop episode recordings + converted datasets — hosted on the HF Hub
# (NoahWeiss/farm_uf850_bottle), not in git. The recorder writes to
# Dataset*/; export_lerobot.py emits datasets_lerobot/.
Dataset*/
datasets/
datasets_lerobot/
*.parquet

# Claude Code runtime lock (per-session, not source)
.claude/scheduled_tasks.lock

# Unity IL2CPP / CMake build intermediates (Quest Android build)
farm-quest/UnityProject/.utmp/

# Playwright session artifacts
.playwright-mcp/
*.png
home*.png run_*.png running_*.png

# Keep checked-in documentation screenshots
!docs/img/*.png
!farm-cloud/ui/public/**/*.png
MUJOCO_LOG.TXT
